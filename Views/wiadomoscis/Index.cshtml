@model IEnumerable<WebApplication19.Models.wiadomosci>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id_konwersacji)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_uzytkownicy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tresc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.data_utworzenia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.czy_usunieta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id_konwersacji)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_uzytkownicy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tresc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.data_utworzenia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.czy_usunieta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_wiadomosci">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_wiadomosci">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_wiadomosci">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
