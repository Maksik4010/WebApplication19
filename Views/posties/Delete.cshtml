@model WebApplication19.Models.posty

@{
    ViewData["Title"] = "Delete";
}

<h1>Usuwanie posta</h1>

<h4>Czy na pewno chcesz usunąć ten post?</h4>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.tresc)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.tresc)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.data_utworzenia)
        </dt>
        <dd class="col-sm-10">
            @{
                var time = Model.getDate((double)(Model.data_utworzenia));
            }
            @Html.DisplayFor(model => model.data_utworzenia)
            </dt>
    </dl>
    <div style="text-align:right">
        <form asp-action="Delete">
            <a asp-action="Index" class="btn btn-primary">Powrót</a>
            <input type="hidden" asp-for="id" />
            <input type="submit" value="Usuń" class="btn btn-danger" />
        </form>
    </div>
</div>
